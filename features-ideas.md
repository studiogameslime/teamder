# רעיונות לשיפור — Teamder
*נוצר במהלך סבב QA מלא, 30 במאי 2026.*
*לא לפיתוח עכשיו לפי הוראת המשתמש — רק רישום.*

---

## 1. ניקוי מודולי "קבוצות/teams" שכבר לא בשימוש
**מצב:** המעבר לטיימר-בלבד (27.05) הסיר את לוגיקת הקבוצות מהמסך החי, אבל הקוד עדיין מלא ב:
- `TeamsOverviewSheet.tsx` — קומפוננטה שלמה ללא imports בכלל (dead code)
- `guestModal.estimatedRating` — שדה שאין לו צרכן אמיתי בקליינט מאחר ש-TeamsOverviewSheet לא בשימוש
- `gameService.getCommunityPlayerStats.wins` — תוקן בסבב הקודם, אבל יש עוד שדות "מתי"
- `liveTeamWinsLabel` ב-i18n — לא בשימוש
- `statsWins`, `statsLosses`, `statsWinPct` ב-i18n — לא בשימוש

**מה לעשות:** סבב cleanup מקיף שמוחק את כל הקבצים והשדות שלא בשימוש, כולל ה-DB fields ב-types/index.ts (TeamColor, MatchRound וכו'). זה יקטין את גודל ה-bundle ויקטין את משטח התחזוקה.

**למה לא עכשיו:** סבב QA הוא לא הזמן לשינויי אדריכלות גדולים. רושם להעתיד.

## 2. אין כפתור "נעילת הרשמה" ב-UI למרות שהפונקציה קיימת בשירות
**מצב:** `canLockRegistration` ו-`lockRegistration` קיימים בקוד (gameLifecycle.ts:192, gameService.ts:3086), אבל אין button או menu item שמפעיל אותם. מנהל לא יכול לנעול הרשמה ידנית — רק אוטומטית אחרי שעת ה-grace.

**מה לעשות:** להוסיף "נעילת הרשמה" לתפריט המנהל של המשחק, בין "ערוך משחק" (אם נראה) ל-"מחיקת משחק". `canLockRegistration` כבר מטפל ב-gating, צריך רק לחבר.

**למה לא עכשיו:** הוראת המשתמש "אל תוסיף פיצ'רים". רושם להעתיד.

## 3. App Check enforcement חוסם בדיקות אורח-משחק על אמולטור
**מצב:** כל הזימונים ל-CFs עם `enforceAppCheck: true` (כמו `ensurePersonalGroup`) נכשלים על אמולטור עם דחיית "תקלה ביצירה — לא הצלחנו להכין את הסביבה. נסה שוב בעוד רגע." הסיבה: בילד פרודקשן משתמש ב-Play Integrity שלא עובד באמולטור.

**מה לעשות:** או להוסיף `EXPO_PUBLIC_APP_CHECK_DEBUG_TOKEN` לבילדים שמיועדים לבדיקות אמולטור (כלומר profile חדש ב-eas.json), או לרשום debug token ב-Firebase Console → App Check ולהפיץ אותו לצוות QA.

**למה לא עכשיו:** דורש שינוי בתצורת הבנייה, יש לדבר עם המשתמש קודם על מסלול QA.

## 4. הצגת המשחק במצב active במסך הראשי
**מצב:** אחרי שמשחק נכנס למצב active (טיימר רץ), הוא נעלם מ"המשחקים שלי" ומ"גלה משחקים" כי שני המסכים מסננים רק status=open. כדי לחזור למסך החי המשתמש צריך נתיב לא אינטואיטיבי.

**הצעה:** באנר קבוע בראש מסך המשחקים — "🔴 משחק חי: בדיקת תיקון, 02:14 רץ" שמוביל ישירות ל-LiveMatchScreen.

**למה לא עכשיו:** דורש עיצוב + רכיב חדש. רושם להעתיד.

## 5. liveMatch.scoreA/scoreB ב-Firestore עדיין נכתבים על-ידי buildShuffledLiveMatch
**מצב:** הפונקציה `buildShuffledLiveMatch` ב-MatchDetailsScreen.tsx:372 עדיין כותבת `scoreA: 0` ו-`scoreB: 0` ב-liveMatch. אלה שדות שאיש לא קורא עוד אחרי הסרת הקבוצות.

**הצעה:** למחוק את השדות מהפונקציה, ו-DB cleanup לכל המשחקים הקיימים שיש להם שדות אלה.

## 6. באג קריטי שתוקן — דירוג שחקנים היה לא נגיש כלל
**מצב לפני התיקון:** PlayerCardScreen היה מציג את `RatingSection` רק ב-`isSelfView` (פרופיל של עצמך). כשמסתכלים על פרופיל של מישהו אחר, מוצגת `PairStatsSection` בלבד — בלי דירוג כלל. כלומר, אם הגעת מהבאנר "התחל לדרג" שבסוף משחק, מתן למשתמש נראה רק "אתם חברים + 2 הגעתם יחד + 2 נרשמתם יחד" — בלי שום דרך לדרג אותו.

**איך מצאתי:** סבב QA, בדיקה של רטינג flow. לחצתי "התחל לדרג" בבאנר, הגעתי לכרטיס של מתן, ולא ראיתי שום כפתור דירוג.

**התיקון:** הוספתי `RatingSection` ל-branch של "viewing OTHER user" ב-PlayerCardScreen.tsx. עכשיו כשרואים את הכרטיס של מישהו אחר עם groupId משותף, מוצג סקציית "דירוג בקבוצה הזו" עם כפתור "דרג" שפותח את ה-RatingModal.

**עדכון:** התיקון מחייב בילד חדש כדי שיעבוד בפועל אצל משתמשים. עד אז — הפיצ'ר הדירוג בלתי-זמין.

