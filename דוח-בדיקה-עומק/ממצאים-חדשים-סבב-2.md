# ממצאים חדשים — סבב בדיקה 2 (סריקה מעמיקה פר-פיצ'ר)

תאריך: 11.07.2026 · שיטה: 20 סוכני-ניתוח מקבילים (פירוט דק פר-פיצ'ר) + שלב סינתזה שמנכה כפילויות מול סבב 1. סה"כ 67 ממצאים גולמיים → 58 ממצאים חדשים (40 מאומתים, 18 חשודים).

הערת-מגבלה: אחד מ-20 הסוכנים (טיימר-משחק-חי) החזיר פלט-בוסר ולא סיפק ממצאים; אזור זה כוסה לעומק בסבב 1 (הטיימר מבוסס שעון-שרת — אומת כתקין). כל שאר 19 הפיצ'רים כוסו במלואם.

---

## ממצאים חדשים — סבב 2

**סיכום:** 58 ממצאים חדשים (לא כפולים לסבב 1) — **40 מאומתים, 18 חשודים**. מוינו 5 כפילים-לסבב-1 (friendRequestAccepted dedup, single-person stale offer, gamePlayersJoined under-count, friend-incoming unreachable, removeGuest no-promote) ומוזגו 3 זוגות חופפים. lat/lng ביצירת מועדון הושמט (ידוע), אך coverImageId הוא ממצא חדש נפרד להלן.

---

### 🔴 קריטי

**[critical·confirmed] כשל חולף בשאילתת games מוחק לצמיתות תגי-הישג שנרכשו** — הישגים
`achievementsService.ts:232-252,497-522,551-570`. שחזור: משתמש ותיק פותח מסך הישגים באופליין/בזמן רענון-טוקן → getDocs(games) זורק. בפועל→צפוי: ה-try/catch בולע ומחזיר gamesJoined=0, deriveCounters מסתיים תקין (ה-fallback לא נורה), diffUnlocks גוזם את התג ו-persistFirebase דורס את כל המערך ומוחק את התג מ-DB — צפוי שמקור-קריאה שנכשל לא ייחשב 0 לגיטימי. שורש: כל מקור עם ברירת-מחדל 0 בכשל במקום סנטינל "לא-ידוע/דלג".

---

### 🟠 חמור

**[severe·confirmed] לקוח מזייף pendingPromotion ומושיב את עצמו ב-players — עוקף reconciler, כרטיס-אדום וקיבולת** — rules
`firestore.rules:717-723,864-872`. שחזור: כתיבה #1 קובעת pendingPromotion.uid=self (דלתא-0, עוברת self-branch), כתיבה #2 מוסיפה self ל-players — clause(d) בוטח בשדה client-writable. בפועל→צפוי: הושבה ללא הצעה אמיתית, ללא maxPlayers-guard, חסום-אדום חוזר; צפוי ש-pendingPromotion יהיה server-only או שהענף יתיר רק null. שורש: שדה client-writable מטופל כ-server-authoritative.

**[severe·confirmed] עריכת משחק: visibility נשמר לפני שאר העריכה — דליפת משחק פרטי לפומבי בכתיבה חלקית** — game-edit
`GameEditScreen.tsx:250-261→273-316`. שחזור: מחליף community→public + מזיז startsAt לחלון מתנגש, שומר; setVisibility מתחייב, updateGameV2 זורק GAME_OVERLAP. בפועל→צפוי: המשחק כבר public בעוד המשתמש רואה "שמירה נכשלה"; צפוי אטומיות. שורש: setVisibility כתיבה נפרדת ללא rollback.

**[severe·confirmed] ביטול בחלון ה-settle (2ש') נבלע — הרקונסיילר מושיב בכל זאת** — הרשמה
`gameService.ts:4897,4190` + `index.ts:2251`. שחזור: "אני מגיע" ואז "בטל" תוך 2ש'. בפועל→צפוי: cancelGameV2 נוגע רק ברוסטר ולא במסמך joinRequests/{uid} ה-queued → הרקונסיילר מושיב את המבטל בדיעבד; צפוי שהביטול ימחק את מסמך הבקשה. שורש: cancelGameV2 לא מתייחס ל-joinRequests.

**[severe·confirmed] ראש-המתנה שבחר "יציאה מהמשחק" (במקום "ויתור") שובר את שרשרת ההצעה — המקום נותר יתום** — waitlist
`gameService.ts:5024-5037` + `index.ts:4246`. שחזור: waitlist=[A,B], A עם הצעה בוחר cancelGameV2. בפועל→צפוי: pendingPromotion=null, departed ריק (A לא היה ב-players) → הטריגר departed.size>0 נכשל, B לעולם לא מוצע; צפוי שרשור לבא-בתור. שורש: cancelGameV2 מאציל לטריגר שמגודר בעזיבת-שחקן בלבד, בלי שרשור עצמאי.

**[severe·confirmed] Filler שאושר ל-waitlist מקבל push "נכנסת" במקום נוסח המתנה** — filler
`index.ts:9778`. שחזור: משחק בתפוסה מלאה (9/10+אורח), אדמין מאשר filler → הטרנזקציה שמה אותו ב-waitlist (bucket:'waitlist') אך ה-push מקודד bucket:'players'. בפועל→צפוי: מקבל "אושרת ל<game>" כאילו שחקן מאושר; צפוי נוסח המתנה. שורש: payload מקודד קשיח, תוצאת goesToPlayers לא נאספת מחוץ לטרנזקציה.

**[severe·confirmed] AchievementsScreen & PlayerCard גוזרים עם groups ריק לא-מהודרד → תגי-קבוצה נגזמים ב-cold open** — הישגים
`AchievementsScreen.tsx:84-116`, `PlayerCardScreen.tsx:677-702` (מול ProfileScreen guard). שחזור: cold-launch/switchAccount ופתיחת "ההישגים שלי" לפני הידרוד. בפועל→צפוי: teamsJoined/Created=0 → תגי communities/founder/coaching נגזמים ונכתבים ל-DB; ProfileScreen מגן ב-`!groupsHydrated`, השניים לא. שורש: חסר hydration-guard לפני persist.

**[severe·suspected] מפתח idempotency קפוא של הסבב (round:updatedAt) חוסם השלמת-סבב אחרי retry/קריסה** — live-advanced/stats
`AdvancedLiveMatchScreen.tsx:436-497`, `gameService.ts:3207-3275,3259-3265` + `index.ts:10664`. שחזור: (א) קריסה/eviction בזמן FillerPickerModal אחרי commit-stats; (ב) אובדן-תגובה בסיום-סבב רגיל. בפועל→צפוי: committedRounds/{N:Tn} כבר קיים אך הרוטציה לא התקדמה → כל "סיים משחקון" נכשל ALREADY_EXISTS, הרוטציה תקועה והגולים אובדים; צפוי זיהוי "כבר בוצע" והתקדמות. שורש: המפתח קופא ברוטציה שלא התקדמה; רק מסלול ביטול-fill מנודג' (nudgeRotationAfterFillCancel), מסלולי הקריסה/finalize-רגיל לא.

---

### 🟡 בינוני

**[medium·confirmed] אורח (guest) מקבל prompt הרשאת-push ורושם token תחת uid אנונימי יתום** — auth
`RootNavigator.tsx:230-239` + `notificationsService.ts:503`. אפקט הרישום ו-requestAndRegisterPushToken חסרי בדיקת isGuest → prompt לגולש-אורח (חיכוך מול App Store 5.1.1) + token יתום. צפוי: לדחות עד הרשמה אמיתית.

**[medium·confirmed] Onboarding שם עוקף sanitizeDisplayString — תווי bidi/impersonation נשמרים** — onboarding
`userService.ts:359` (completeOnboarding עושה רק trim) מול 650 (updateProfile מסנן). המסלול הראשי שבו כל משתמש חדש קובע שם כותב גולמי; הניקוי רץ רק בעריכה מאוחרת. rules מגבילים אורך בלבד.

**[medium·confirmed] משתמש חסום (block) עדיין שולח בקשת-חברות ומקפיץ push לחוסם** — friends
`firestore.rules:583-592` + `index.ts:9872`. החסימה נאכפת רק בצ'אט; friendRequests-rule ו-onFriendRequestCreated לא בודקים blocked → הטרדה דרך בקשות. צפוי: חסימה תמנע גם בקשת-חברות.

**[medium·confirmed] דירוגי-עבר (1–10) מוצגים גולמיים אך הכוחות מאזנים חצי — התצוגה ≠ הבסיס לחלוקה** — ratings
`rating.ts:20`, `MatchDetailsScreen.tsx:2121-2134`, `GuestModal.tsx:273`, `firestore.ts:509-516`. צ'יפ/סליידר/ממוצע-קבוצה מציגים 8.0 על סקאלת 0–5 בעוד balanceTeams מריץ normalizeRating (חצי). צפוי: תצוגה דרך אותו normalizeRating. תלוי-נתונים: רק היכן שקיימים ערכי-עבר >5.

**[medium·confirmed] בקשת-חברות נכנסת חסומה בכרטיס כשקיים doc 'declined' יוצא ישן (קדימות declined על incoming)** — friends
`friendsService.ts:358-363`. getRelationship קורא את ה-doc היוצא A__B, מוצא declined→'rejected' ומחזיר לפני קריאת B__A הנכנס → הכפתור "נדחתה" מושתק, A לא יכול לאשר. ה-doc ה-declined אינו ניתן לניקוי מה-UI. (מוסיף מנגנון declined ל-round-1 "friend-incoming unreachable".)

**[medium·confirmed] ייחוס "אושר ע"י" בציר-הזמן נכשל שקט לחברים במספר מועדונים** — join-audit
`groupService.ts:972`, `firestore.rules:552-556`. getMemberApprover מריץ query(where userId==X) בלי groupId; אם ל-X בקשה במועדון שהצופה לא-אדמין שלו → Firestore דוחה את כל ה-list → מחזיר null, שם המאשר לא מוצג. שובר את הפיצ'ר לקהל היעד (אדמינים).

**[medium·confirmed] כרטיס "מלך השערים" ו-fun-fact בוחרים את מוביל-הנקודות, לא מוביל-הגולים** — stats
`CommunityStatsScreen.tsx:173-175`, `championship.ts:99-105`. getCommunityChampionship ממיין sortBy='points' אך topScorer עדיין לוקח players[0] (הערה מיושנת "כבר ממוין לפי גולים"); שאר המובילים משתמשים ב-leaderBy. כש-מוביל-נקודות≠מוביל-גולים הכרטיס ו-kingSharePct שגויים.

**[medium·confirmed] כל מועדון חדש נוצר ללא תמונת-רקע — coverImageId נזרק בדרך ל-callable** — community-create
`groupService.ts:407-420` + `index.ts:7049-7065`. ה-payload ל-createGroupCallable לא כולל coverImageId (וגם lat/lng, ידוע), וה-interface/groupDoc בשרת לא מכירים אותו → כותרת ריקה לכל מועדון חדש עד בחירה ידנית.

**[medium·confirmed] עריכת מועדון ללא cap כופה בשקט maxMembers=40, וקהילות >40 חברים תקועות בלולאת דחייה** — community-edit
`CommunityEditScreen.tsx:67-70`, `GroupWizardForm.tsx:91`, `groupService.ts:1129-1141`. original.maxMembers=undefined→'40'; שמירה כותבת cap 40; אם >40 חברים ה-guard זורק GROUP_MAX_BELOW_CURRENT וה-revert מחזיר '40' → כל עריכה נדחית. אין הבחנה "אין cap" מ-"40".

**[medium·confirmed] עדכון מחזיקי-ציוד = last-writer-wins על כל המערך; שני אדמינים דורסים זה את זה** — equipment
`CommunityPlayersScreen.tsx:262-300` + `groupService.ts:812-816`. read-modify-write של ballHolderIds מ-state מיושן במקום arrayUnion/arrayRemove אטומי → סימון אדמין A נמחק ע"י B (בניגוד להערה "never disturbing anyone else's mark").

**[medium·confirmed] visibility במשחק scheduled/locked נכשל בשגיאה גנרית ומאבד את כל שאר העריכות** — game-edit
`gameService.ts:5996`, `GameEditScreen.tsx:250-261`. setVisibility זורק Error רגיל (ללא code) כש-status!=='open'; ה-wizard מציג gameWizardSubmitFailed ו-updateGameV2 לא רץ → זמן/מגרש/notes נזרקים, אף שהמתג מוצג פעיל.

**[medium·confirmed] חסימת כרטיס-אדום עוקפת/מטעה — נתיב ההצטרפות מ-GamesList (וגם MatchDetails ב-cold)** — join/discipline
`GamesListScreen.tsx:351` + `MatchDetailsScreen.tsx:417-431,864` + `index.ts:2400`. handleCardPrimary קורא requestJoinGame ללא בדיקת redBlocked; ב-MatchDetails grp=myCommunities.find עלול להיות undefined→cardsEnabled=false→redBlocked=false. הלקוח מציג "נרשמת"+קונפטי, הרקונסיילר דוחה RED_CARD_ACTIVE על מסמך joinRequests שאיש לא מנוי עליו → נעלם אחרי 2.5ש' בלי הודעה. (מוזגו שני מסלולי red-card.)

**[medium·confirmed] predictBucket אופטימי מדי בעומס → חגיגת "נרשמת" שקרית בעוד המשתמש בהמתנה** — join
`gameService.ts:4197` + `MatchDetailsScreen.tsx:967-971`. predictBucket מריץ assignJoins עם snapshot מקומי בודד ומנבא 'players' לרוב הלוחצים; הרקונסיילר ממיין לפי tappedAt ומעביר עודף ל-waitlist. רחב מ-round-1 "registered toast on waitlist" (כל עומס, לא רק pendingPromotion).

**[medium·confirmed] קידום אורח-בהמתנה מתעלם ממושב שמור (pendingPromotion) → חריגה מהקיבולת** — guests
`gameService.ts:7076` + `MatchPlayersScreen.tsx:769-771`. גם ה-gate של "להרכב" וגם adminReorderGuests סופרים players+activeGuests ומשמיטים pendingPromotion; אורח ממלא מושב שמור, אז confirmSpotOffer מוסיף את המאשר ללא re-check → 11>10. השרת (adminReorderRoster) כן סופר offerHeld — פער עקביות.

**[medium·confirmed] cloneRecurringGames ללא future-guard → סופת clones מהעבר + ספאם push חוזר** — recurring
`index.ts:2807-2919,2951-2963`. משחק חוזר עם kickoff >שבוע בעבר (מסך היצירה מתיר): ה-clone נוצר ב-source+1wk שעדיין בעבר, status='open', נשלח push; ה-clone עצמו חוזר/בעבר → clone הבא, שרשרת clones ריקים כל אחד יורה push. צפוי: `while nextStartsAt<=now advance`, מופע עתידי יחיד.

**[medium·confirmed] skipRecurringWeek מתעלם מ-recurringNextCreatedAt → משחק כפול לשבוע הבא, הסדרה מכפילה כל מופע** — recurring
`gameService.ts:3734-3781`, `MatchDetailsScreen.tsx:1600,1981`. משחק חוזר עבר kickoff+3h ב-open, ה-CF כבר יצר clone דטרמיניסטי; "מחק רק השבוע" יוצר clone שני (id אקראי, recurring:true) בלי לבדוק את הלאץ' → שני מופעים לשבוע הבא, שניהם מכפילים. גם +7d שטוח (לא DST).

**[medium·confirmed] מסך הזמנה ידני מדלג על מי שבחר "פנוי בכל יום" (preferredDays ריק)** — filler
`userService.ts:489,542`. findAvailablePlayers משתמש ב-array-contains ו-includes(day) שמוציאים מערך ריק, בעוד ה-backend (index.ts ~8642/~9478) מתייחס לריק כ"כל יום" ומספֵר אותם בלוח-הבית → סתירה בין הספירה למסך.

**[medium·confirmed] מסך הזמנה ידני מתעלם מ-preferredTimes ומציע משחקים מחוץ לחלונות שנבחרו** — filler
`userService.ts:517`. ה-guard `if(opts.hour && a.timeFrom && a.timeTo)` נדלג כי timeFrom/timeTo מיושנים (לא נכתבים ע"י ה-UI הנוכחי), ו-preferredTimes לעולם לא נבדק → משתמש "בוקר בלבד" מוצע למשחק ערב 20:00.

**[medium·confirmed] Campaign 'hasPush' קורא fcmTokens ישן ב-root → כמעט אף אחד לא מתאים (קהל+preview שגויים)** — notifications
`adminUserPush.ts:341,135`. hasPush נגזר מ-root .fcmTokens שכבר לא נכתב (הטוקנים ב-/private/push). hasPush=true מתאים רק legacy (≈0), hasPush=false מתאים כמעט לכולם. delivery (tokensFor) קורא private-עם-fallback, לכן רק הפילטר מיושן.

**[medium·confirmed] דחיית משחק לזמן מאוחר יותר אחרי שתזכורת-1ש' כבר נורתה מדכאת את התזכורת ל-kickoff החדש** — notifications
`index.ts:1763,1873` + `gameService.ts:2664`. reminderSent הוא לאץ' חד-כיווני שמתאפס רק ב-clone השבועי; עריכת startsAt כותבת {...patch,updatedAt} בלי לנקותו → sendGameReminderForGame מחזיר false. מחמיר עם bucket ה-dedupe של 6ש'.

**[medium·confirmed] push "בקשתך נדחתה" מנווט למסך CommunityDetails החברים-בלבד → קריאה נדחית, דף שבור** — deeplinks
`navigationRef.ts:228-247`, `CommunityDetailsScreen.tsx:148-197`, `firestore.rules:240-245`. navigateForPush('rejected') מנווט ל-CommunityDetails; groupService.get נכשל permission-denied (הנמען אינו חבר), reload עושה setLoading(false) בלי setGroup → ספינר/דף ריק. צפוי: CommunityDetailsPublic כמו navigateInvite. אין הבחנת member/public בענף.

**[medium·suspected] נעילה קבועה מ-re-request אחרי שיקום: doc 'rejected' לעולם לא מנוקה** — join
`groupService.ts:2081-2096,903-926`. אחרי rejectMember→doc 'rejected', הזמנה דרך inviteFriendsToGroup עוקפת writeJoin, X מצטרף ואז עוזב; בקשה עצמאית חוזרת מוצאת את ה-'rejected' וזורקת GroupJoinRejectedError → חסום לצמיתות. אין ניקוי 'rejected' על הצטרפות/עזיבה.

**[medium·suspected] אין בדיקת חברוּת/visibility בנתיב ההצטרפות — לא-חבר שיודע gameId מצטרף למשחק פרטי** — join/auth
`firestore.rules:1112-1123` + `index.ts:2251`. create-rule של joinRequests דורש רק isSelf+queued; reconcileGameJoins לא בודק חברוּת. משתמש עם gameId מודלף כותב joinRequest ומושב אם open+!requiresApproval. אכיפת החברוּת קיימת רק ב-read של מסמך המשחק.

**[medium·suspected] "התחל משחקון" משאיר שעון על 00:00 מושהה — commitFilledRotation(resetTimerBy) מאפס את startTimer** — live-advanced
`AdvancedLiveMatchScreen.tsx:626-646,484-490` + `gameService.ts:3501-3512`. onStartRound קורא startTimer ואז advanceFillFlow עם resetTimerBy (כי !keepClock) → _persistRotation מאפס timerRunning=false/accumulated=0 → השעון עומד. צפוי (לפי ההערה): להתחיל ישר ל-running.

**[medium·suspected] רטרו-גול נספר פעמיים על retry (idempotency key מתחדש בכל שמירה)** — retro-goals
`RetroGoalsSheet.tsx:86`, `gameService.ts:1219`, `index.ts:10967`. addRetroGoal מצליח בשרת אך התגובה אובדת; retry מייצר retroGoalId חדש (Date.now+random) → הגול נזקף שוב (goals/communityPlayerStats/gamePlayerStats, וגם למבשל). בניגוד ל-commitRoundStats שמפתחו יציב.

---

### 🟢 מינורי

**[minor·confirmed] ביטול התחברות Google מציג דיאלוג "שגיאה" בעוד Apple מושתק** — auth. `SignInScreen.tsx:64-73` מול 111-113. ה-appAlert במסלול Google ללא תנאי !cancelled.

**[minor·confirmed] זיהוי provider בהתנגשות אימייל = dead code תחת email-enumeration protection** — auth. `auth.ts:180-193`, `EmailAuthScreen.tsx:102-107`. fetchSignInMethodsForEmail מחזיר [] → אין הכוונה ל-Google/Apple, המשתמש מקבל "פרטים שגויים" מבלבל.

**[minor·confirmed] עריכת שם-עיר לא מבצעת geocode מחדש — קואורדינטות נשארות מהעיר הישנה** — community-edit. `CommunityEditScreen.tsx:96-121`. submit לא קורא geocodeCity ו-updateGroupMetadata לא מעדכן lat/lng. סמוי כרגע כי גם ביצירה lat/lng נזרק.

**[minor·confirmed] סריקת ניקוי בקשות-join ב-leaveGroup נדחית תמיד עבור העוזב** — leave. `groupService.ts:1452-1485`, `firestore.rules:570-571`. update דורש isGroupAdmin; העוזב כבר לא אדמין → permission-denied נבלע. dead best-effort. אין ענף שמתיר userId==auth.uid לסגור בקשתו.

**[minor·confirmed] היוצר לא מקבל joinedAt/adminSince — חסרות אבני-דרך בציר-הזמן שלו** — timeline. `index.ts:10283-10318`. stampMembershipDates הוא onDocumentUpdated ולא נורה על create; היוצר כבר במערכים. קוסמטי.

**[minor·confirmed] validateAutoTeamsAt חוסם כל עריכה כשזמן-הכוחות כבר עבר** — game-edit. `GameWizardForm.tsx:369-380,403-405`. submit מריץ ולידציה על autoTeamsAt שמור שלא נגעו בו → wizardAutoTeamsInPast חוסם שמירת notes. צריך לבדוק רק ערך ששונה.

**[minor·confirmed] מוזמן למשחק requiresApproval מקבל toast "ממתין לאישור" אף שהושב ישירות** — join. `gameService.ts:4069` מול `index.ts:2415`. predictBucket לא מתחשב ב-invitedUserIds ומנבא 'pending'; הרקונסיילר מדלג על אישור למוזמנים ומושיב כ-'players'.

**[minor·confirmed] adminAddPlayers לא מנקה cancellations של שחקן מוסף-מחדש → כרטיס-צהוב פנטום** — discipline. `index.ts:6259-6284`. הטרנזקציה לא מוחקת cancellations[P] (בניגוד ל-joinGameV2:4465-4475) → getPlayerDisciplineSnapshot סופר yellow למרות שהגיע ושיחק.

**[minor·confirmed] skipRecurringWeek נודד ב-DST ומשמיט auto-teams/filler-push לעומת ה-CF clone** — recurring. `gameService.ts:3738-3779`. +7*24h שטוח (20:00→21:00/19:00) ו-createGameV2 משמיט autoTeamsAt/autoTeamsMethod/acceptsFillerPush. צריך parity עם cloneRecurringGames.

**[minor·confirmed] cancellations/joinedAt נכתבים ללא ולידציה בענף ה-self — griefing השתקת-תזכורות וזיוף זמנים** — rules. `firestore.rules:717-723` + `index.ts:1904`. אין הגבלת diff ל-auth.uid (בשונה מ-draftTeamFeedback); תוקף מזריק cancellations[victim] כדי להשתיק תזכורות.

**[minor·confirmed] ballBringerIds: כתיבה בדלתא-גודל-0 מאפשרת החלפת חברים במערך (זיוף דגל "מביא כדור")** — rules. `firestore.rules:879-903`. שני ה-±1 ternaries מדלגים כשהגודל נשמר, אין membership/caller-guard → משתתף משכתב את הדגל של אחרים.

**[minor·confirmed] /errors פתוח ליצירה/עדכון ללא auth ועם count לא-מולידצי — זיהום/דריסת מסמכי-שגיאה** — rules. `firestore.rules:1591-1607`. update מותר ל-fingerprint נבחר-לקוח, count ללא ולידציה → אנונימי דורס הודעות/stacks ומנפח count.

**[minor·suspected] ensureNativeAuthMirror רץ גם ל-session אורח אנונימי → session native אמיתי בזמן גלישת-אורח** — auth. `auth.ts:450,281-306`. אין בדיקת isAnonymous; אם cache Google לא נוקה, ה-widget/watch פועלים כ-Google בזמן שה-UI אורח. נדיר.

**[minor·suspected] שם מתווי zero-width/bidi בלבד יכול לרוקן את שם-התצוגה בעריכה** — profile. `ProfileEditScreen.tsx:67`, `userService.ts:651-665`. trim לא מסיר zero-width → canSave true, sanitize מקריס ל-'' שנכתב; rules מתירים ריק. צריך re-validate non-empty אחרי sanitize.

**[minor·suspected] עריכת פרופיל בתהליך נדרסת בשקט ע"י snapshot רקע של user-doc** — profile. `ProfileEditScreen.tsx:51-56`. effect ה-reset (deps: name/photoUrl/avatarId) נורה על עדכון חי ומאפס את הטופס תוך כדי הקלדה, ללא הבחנה בין save-propagation לעדכון חיצוני.

**[minor·suspected] הגדרת hideInternalRating כמעט no-op — דירוג אישי חסום ל-admin בכל מקרה** — ratings. `CommunityPlayersScreen.tsx:153,479`, `MatchPlayersScreen.tsx:201`. showRating דורש iAmAdmin; hideInternalRating משפיע רק על שורת ממוצע-הקבוצה. ייתכן מכוון — מטעה את האדמין.

**[minor·suspected] createGroupCallable מקדם את מונה ה-rate-limit לפני ולידציה/commit — יצירה כושלת שורפת מכסה** — community-create. `index.ts:7198-7222` לפני 7227/7317. אין compensation; commit שנכשל שורף אחת מ-5 היצירות היומיות.

**[minor·suspected] updateGroupMetadata משתמש ב-batch.update על /groupsPublic — אם המראה חסר, כל העריכה נכשלת** — community-edit. `groupService.ts:1215-1224`. batch.update דורש מסמך קיים; מועדון legacy/hidden ללא mirror → not-found מבטל גם את /groups. צריך set(merge).

**[minor·suspected] תזמון autoTeamsAt/publicOpenAt לא ניתן לניקוי כשהפקד מוסתר אך הערך נשמר** — game-edit. `GameWizardForm.tsx:970,916`. internalRating כבוי → קבוצת autoTeams לא מוצגת אך values.autoTeamsAt נטען ונשלח; רינדור מותנה לא מאפס state.

**[minor·suspected] הצעות שפגו על משחק 'locked' לעולם לא מואצות — מקום נשאר שמור-ריק** — waitlist. `index.ts:3106,3121`. runExpireStaleOffers מדלג על status!=='open' בעוד confirm/promote תומכים ב-'locked'. חלון צר.

**[minor·suspected] שחרור כמה מושבים בכתיבה בודדת מקדם רק ממתין אחד; שאר המקומות נשארים ריקים** — waitlist. `index.ts:4237-4272`. הטריגר ממלא מושב אחד לכל firing; ה-re-fire נטול departures (size===0). אין לולאת-מילוי. גם העלאת maxPlayers לבד לא מפעילה (rosterShrank=false).

**[minor·suspected] cloneRecurringGames בהתאוששות ALREADY_EXISTS לא שולח push למשחק שנפתח מיד** — recurring. `index.ts:2933-2947` (`continue` מדלג על 2951-2963). קריסה בין create ללאץ' → sweep הבא מתנגש ALREADY_EXISTS ו-continue מדלג על ה-notify (createNotificationOnce ממילא dedupe).

**[minor·suspected] promoteOrphanToGroup לא מוודא ש-fromGameId שייך לקבוצה → סטטיסטיקות של משחק זר נזרעות ושורות cps אמיתיות נמחקות** — orphan-promote. `index.ts:6705-6710,6794-6880`. fromGameId רק length-clamped; ה-reset קורא gamePlayerStats של משחק זר ומוחק כל cps row שלא במפה. ענף ה-derive כן scoping ל-groupId, ה-explicit לא.

**[minor·suspected] Campaign שקורס/timeout באמצע שליחה תקוע ב-'sending' לנצח ולא retry** — notifications. `adminUserPush.ts:266` מול sweep 427. sweepDueCampaigns בוחר רק status=='queued'; אין reaper ל-'sending', ו-`sendAt<=now` מחריג מסמכים בלי sendAt.

**[minor·suspected] tap על push ב-cold-start לפני MainTabs מפיל בשקט את הכוונה** — deeplinks. `App.tsx:718-741`. handleResponse ממתין רק ל-isReady() (כל navigator); ב-AuthStack (משתמש מנותק/פרופיל-חסר) navigate('GameTab') לא קיים → "action NAVIGATE was not handled", navigate לא זורק/מחזיר false → הכוונה אובדת ללא stash/retry.

**[minor·suspected] "declined terminal" של בקשת-חברות עקיף ברמת rules דרך delete+create — ספאם** — rules. `firestore.rules:588-618`, `friendsService.ts:136-152`. update declined→pending חסום, אך delete+create חדש עוקף; האינווריאנט נאכף רק בלקוח (getDoc-check) שלקוח מזויף מדלג.
