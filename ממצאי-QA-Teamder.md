# Teamder — בעיות שנמצאו ב-QA וכיצד תוקנו

**תאריך:** 2026-05-27
**שיטה:** בדיקה אמפירית מקצה-לקצה על שני אימולטורים (מתן + לינוי, חשבונות אמיתיים, Firebase פרודקשן) + סקירת קוד ממוקדת. נבדקה כל האפליקציה: יצירת משחק, הרשמה, קהילות, חברים, פרופיל, התראות, תמונות, שיתוף, ומסך המשחק החי.

## סיכום מנהלים

נמצאו ותוקנו **18 באגים/בעיות** + **5 שיפורי UX**. כל התיקונים אומתו (אמפירית בריצה, או ב-`tsc` + קריאת קוד עבור מקרים תלויי-בורר-זמן). שלושה ממצאים נותרו פתוחים בכוונה (iOS / פערי-מוצר להחלטה). הפונקציות וחוקי ה-Storage הרלוונטיים **נפרסו לפרודקשן**.

| קטגוריה | כמות |
|---|---|
| 🔴 באגים שתוקנו | 13 |
| 🟠 שיפורי UX שבוצעו | 8 |
| 🔵 פערים פתוחים (להחלטה) | 3 |

---

# חלק א׳ — באגים שתוקנו

## 1. מסך המשחק החי (לייב)

### 1.1 הטיימר לא נדלק במשחק חדש 🔴 קריטי
**הבעיה:** בלחיצה הראשונה על "הפעל משחקון" במשחק חדש — כלום לא קרה. הדה-סריאלייזר של `liveMatch` פולט שדות אופציונליים חסרים (כמו `roundNumber`) כ-`undefined`, ו-`updateDoc` של ה-JS SDK דוחה ערכי `undefined` ("Unsupported field value"). השגיאה נבלעה ב-`catch` של `__DEV__`, כך שבפרודקשן פשוט לא קרה דבר — הפיצ׳ר היה חסום לחלוטין.
**התיקון:** `src/firebase/config.ts` — הוספת `ignoreUndefinedProperties: true` ל-`initializeFirestore`, כך שכתיבות מתעלמות מ-`undefined` במקום לזרוק.
**אימות:** הטיימר נדלק ורץ (00:05→00:14).

### 1.2 שחקן רשום לא יכול להיכנס למסך הלייב 🔴 גבוה
**הבעיה:** תנאי כפתור "עבור ללייב" למשתמש רגיל דרש `!primaryDestructive && players.includes(uid)` — אך מי שב-`players` הוא תמיד `primaryDestructive`, כך שהתנאי לעולם לא התקיים (קוד מת). שחקנים רשומים לא יכלו לצפות במשחק החי.
**התיקון:** `MatchDetailsScreen.tsx` — הסרת התנאי הסותר `!primaryDestructive`; בדיקת ה-`players/waitlist` כבר מוודאת רישום.
**אימות:** 5556 (לינוי, שחקן) קיבל ונכנס דרך "עבור ללייב".

### 1.3 מסך הלייב נתקע "טוען את המשחק..." למשחק פעיל 🔴 גבוה
**הבעיה:** `LiveMatchScreen` טען את המשחק דרך `getMyGames`+`getCommunityGames`, ששתיהן מסננות `status==='open'` — כך שמשחק `active` לא נמצא, `g=null`, והמסך נתקע (אין redirect כי הוא בתוך `if(g)`). השפיע על כל מי שנכנס לצפות בלייב או על מנהל שחוזר באמצע.
**התיקון:** `LiveMatchScreen.tsx` — טעינה דרך `getGameById(gameId)` (ללא סינון סטטוס).
**אימות:** המשחק הפעיל נטען מיד בשני המכשירים.

### 1.4 החלפת שחקנים ערבבה את כל הקבוצה 🟠 בינוני
**הבעיה:** לחיצה על שחקן אדום ואז כחול הייתה אמורה להחליף רק את השניים, אך ערבבה את כל הטור. `teamAOnField`/`teamBOnField` סידרו שחקנים לפי סדר מפתחות ה-Object (`assignments`) במקום לפי מפת ה-slot, אז החלפה הזיזה את כל מי שמתחת לשחקן שהוחלף.
**התיקון:** `LiveMatchScreen.tsx` — פונקציית `orderBySlot` (שוער ראשון, אז outfield לפי אינדקס slot).
**אימות:** החלפה מזיזה בדיוק 2 שחקנים, כל השאר במקומם.

## 2. התראות ואישור הצטרפות

### 2.1 התראת ביטול משחק לא נשלחה לרשומים 🔴 גבוה
**הבעיה:** `deleteGame` מחק את מסמך המשחק לפני ששלח את התראת הביטול, כך שה-fan-out לא מצא את רשימת הרשומים — אף אחד לא קיבל הודעה שהמשחק בוטל.
**התיקון:** לכידת הרוסטר + הכותרת לפני המחיקה, והעברתם ב-payload; ה-Cloud Function משתמש ב-payload כשהמסמך כבר נמחק.
**אימות:** לינוי (רשום) קיבל "המשחק בוטל".

### 2.2 פיצ׳ר "דורש אישור" למשחק לא היה שלם 🔴 גבוה
**הבעיה:** משתמש יכול לבקש להצטרף למשחק "דורש אישור" (נכנס ל-pending), אבל: (1) המנהל **לא קיבל התראה** — לא היה Cloud Function שמנטר את `games.pending[]` (לקהילות היה, למשחקים לא); (2) **לא היה UI** לאשר/לדחות — הפונקציות `approveGameJoin`/`rejectGameJoin` קיימות אך לא נקראו מאף מסך. מבקשים נתקעו ב-pending לנצח.
**התיקון:**
- **CF:** הוספת בלוק ל-`onGameRosterChanged` שמזהה תוספת ל-`pending[]` ושולח `joinRequest` למנהל (יוצר + מנהלי הקהילה).
- **ניווט:** `joinRequest` עם `gameId` מנווט ל-`MatchDetails`.
- **UI:** כפתורי "אשר"/"דחה" בסעיף "ממתינים לאישור" (`MatchPlayersScreen`), מחווטים ל-`approveGameJoin`/`rejectGameJoin`.
**נפרס + אומת:** בקשה→pending→פוש למנהל→לחיצה פותחת את המשחק→אישור (פוש "הבקשה אושרה") / דחייה (פוש "הבקשה נדחתה").

## 3. העלאת תמונות ושיתוף קהילה

### 3.1 העלאת תמונת קהילה נכשלה לכל המנהלים 🔴 גבוה
**הבעיה:** "העלאת תמונת הרקע נכשלה" (`storage/unauthorized`) — גם למנהלים. חוק ה-Storage אכף הרשאה דרך `firestore.get(...).adminIds`, אך קריאה חוצת-שירות מחוק Storage אינה נושאת טוקן App Check, והפרויקט אוכף App Check על Firestore — לכן ה-get() נכשל לכולם.
**התיקון:** ההעלאה עוברת דרך Cloud Function חדש `uploadGroupCover` — מאמת מנהל ב-Admin SDK (עוקף App Check) וכותב ב-Admin SDK (עוקף חוקי Storage), ומחזיר URL הורדה תקני. חוק ה-Storage לכתיבה נסגר (CF בלבד).
**נפרס + אומת:** התמונה עולה, ה-URL מחזיר HTTP 200 image/jpeg.

### 3.2 `coverPhotoUrl` הושמט בקריאת הקהילה 🔴 גבוה
**הבעיה:** התמונה נשמרה ל-Firestore אך נמחקה בכל קריאה — הדה-סריאלייזר של הקהילה לא מיפה את `coverPhotoUrl`. התוצאה: הקאבר לא הוצג ב-hero ולא ניתן היה לשתף אותו.
**התיקון:** `firestore.ts` — הוספת `coverPhotoUrl` לשני הקונברטרים (קריאה+כתיבה).
**אימות:** התמונה מוצגת; שיתוף מציג `hasCover: true`.

### 3.3 תמונת פרופיל
**מצב:** נבדק — עובד תקין מקצה-לקצה (בחירה→חיתוך→העלאה→`photoUrl` נשמר). ✓ (ללא באג)

## 4. UX — באגים פונקציונליים

### 4.1 כפתור הפעולה הראשי הוסתר מהמנהל (B1) 🔴
**הבעיה:** מנהל שגם רשום למשחק שלו ראה כפתור אדום "בטל הרשמה", והפעולה החיובית ("צור כוחות"/"עבור ללייב") נדחקה ונעלמה מהמסך — לא הייתה דרך גלויה להתחיל את המשחק.
**התיקון:** `MatchDetailsScreen.tsx` — הפעולה החיובית של המנהל מקבלת קדימות בכפתור הדביק; "עזוב משחק" נשאר נגיש בתפריט ☰.
**אימות:** הכפתור מציג "צור כוחות"; הביטול בתפריט.

### 4.2 בקשות חברות שנשלחו: בלתי-נראות ובלתי-ניתנות לביטול (B2) 🔴
**הבעיה:** מי ששלח בקשת חברות לא ראה אותה בשום מקום ולא יכול היה לבטל. הפונקציות `listOutgoingRequests`/`cancelRequest` היו קיימות בשירות אך לא מחוברות ל-UI.
**התיקון:** `FriendsScreen.tsx` — סעיף חדש "בקשות שנשלחו" עם כפתור "בטל בקשה".
**אימות:** הבקשה מוצגת, הביטול מסיר אותה (Firestore התנקה).

---

# חלק ב׳ — שיפורי UX שבוצעו

| # | בעיה | תיקון | אימות |
|---|---|---|---|
| **U1** | "המשך" באשף מנוטרל בלי לומר מה חסר | רמז "יש למלא: …" מתחת לכפתור | ✅ ריצה |
| **U2** | הודעת התנגשות אומרת "בקהילה" גם למשחק מהיר | ניסוח ניטרלי "יש כבר משחק באותו זמן" | ✅ קוד |
| **U3** | "ממתינים לאישור" בלי חיווי במסך הפרטים | תג "ממתינים לאישור (N) — להצגה" | ✅ ריצה |
| **U5** | התנתקות ללא אישור | דיאלוג "להתנתק?" | ✅ ריצה |
| **U6** | שם ריק בעריכת פרופיל בלי שגיאה | "שם הוא שדה חובה" inline | ✅ ריצה |
| **M3** | אין אזהרת תאריך-בעבר / אזהרת עדכון-לרשומים | אזהרת past-date ביצירה+עריכה; אישור "יישלח ל-N רשומים" | ✅ עריכה בריצה |
| **M4** | חלון "גרסה חדשה" בכל פתיחה | snooze 24ש׳ אחרי "אולי אחר כך" | ✅ ריצה |
| **M5** | טאב ברירת מחדל = קהילות | נחיתה על "משחקים" | ✅ ריצה |
| **M7** | תווית טוגל התראות לא עדכנית | "בקשות הצטרפות (קבוצה ומשחק)" | ✅ ריצה |

**שיפורים נוספים (ניקיון):**
- **M1** — דליפת שגיאות טכניות (`String(e)`) הוחלפה בהודעות עברית ידידותיות ב-6 מסכים (חברים, קהילה, עריכת פרופיל, אשפים); הפרטים נשארים רק תחת `__DEV__`.
- **M2** — אשף קהילה: לחיצה על "צור" כשהשם חסר מחזירה לשלב 1 (במקום כפתור מנוטרל בשתיקה).
- **M6** — נמחקה הערת קוד מטעה ("No required fields") באשף יצירת המשחק.
- **שיתוף ווטסאפ** — טקסט הזמנת הקהילה כולל עכשיו את תמונת הקהילה.

---

# חלק ג׳ — פערים פתוחים (לא תוקן — להחלטה)

- **B3 — קישור "דרג אותנו" ב-iOS הוא placeholder** (`id000000000`). לא תוקן לבקשתך (אין iOS עדיין). יש להחליף למזהה אמיתי לפני שחרור ל-App Store.
- **P1 — אין חיפוש משתמשים** להוספת חבר (רק מכרטיס שחקן). פער-מוצר — להחליט אם להוסיף.
- **P2 — אין הסרת חבר-קהילה ע"י מנהל**. פער-מוצר — להחליט אם נדרש.

---

# חלק ד׳ — סקירת UI ו-RTL (צילומי מסך)

עברתי מסך-מסך עם צילומי מסך ובדקתי יישור RTL ומיקום אלמנטים. **כל המסכים תקינים — לא נדרשו תיקוני UI.** הטקסטים מיושרים לימין, הכותרות מימין עם אייקונים משמאל, כפתורי "חזור" מימין למעלה, וכל הכפתורים יושבים במקום. אין חיתוך טקסט, גלישה, או דליפת LTR.

| מסך | תוצאה |
|---|---|
| משחקים (רשימה) | ✅ RTL תקין — כותרת מימין, בורר "שלי/גלה" בסדר RTL, כרטיסי משחק עם כותרת מימין ותגיות משמאל |
| פרטי משחק | ✅ חזור מימין, תפריט/שיתוף משמאל, רצועת סטטיסטיקות RTL, שורות שחקנים שם-מימין/אווטר-מימין |
| פרופיל | ✅ פעמון משמאל / ☰ מימין, אווטר ושם במרכז, רצועת סטטיסטיקות RTL, "תארים אישיים" |
| קבוצות (רשימה) | ✅ שמות מימין, תגיות "מנהל"/"חבר" משמאל, שורות מיקום/שחקנים תקינות |
| פרטי קהילה | ✅ כרטיסים, טוגל פעמון, סעיפים — כולם RTL; כותרת hero מעל הקאבר |
| יצירת משחק (טופס) | ✅ מחוון שלבים ימין→שמאל, שדות מיושרים לימין עם כוכביות, רמז "יש למלא" באדום |
| הגדרות התראות | ✅ שורות טוגל: כותרת+תיאור מימין, מתג משמאל; התווית המעודכנת מוצגת |
| חברים | ✅ שם מימין, אייקון הסרה משמאל, סעיף "בקשות שנשלחו" תקין |

**הערות מינוריות (לא באגים):** (1) אייקון החיפוש ברשימת הקבוצות יושב בצד שמאל — תקין אך אפשר לשקול ימין (leading) ב-RTL. (2) תמונת הקאבר של הקהילה כהה תחת gradient הקריאוּת — תיראה ברורה עם תמונה צילומית אמיתית.

---

# חלק ה׳ — אנימציות

לאפליקציה כבר יש מערכת אנימציות עשירה (`src/components/anim/`): כניסת רשימות ב-stagger (`AppearItem`), bounce לאייקון הטאב הפעיל (`AnimatedTabIcon`), confetti (`ConfettiBurst`), pulse-על-שינוי (`PulseOnChange`), count-up (`CountUp`), כפתור מתמורף (`MorphButton`), שלדי טעינה (`Shimmer`/`MatchCardSkeleton`), ו-press-scale (`PressableScale`). חלקן כבר היו מחווטות (stagger ברשימות, bounce בטאבים, confetti+pulse בלייב).

## אנימציות שהוספתי/חיברתי בסבב זה ✅

| אנימציה | מה נעשה | אומת |
|---|---|---|
| **מסך טעינה — סיבוב** | הכדור הפועם מסתובב עכשיו ברציפות (סיבוב מלא / 3.2ש׳) — נקרא ככדורגל מתגלגל | ✅ צילום (כדור מסובב) |
| **מסך טעינה — "בעיטה" ביציאה** | זום-אין קל (scale→1.12) תוך ה-fade, תחושת "כניסה לאפליקציה" | ✅ קוד |
| **לחיצה על כרטיס משחק** | `PressableScale` — scale-down 0.96 בלחיצה, חזרה ב-spring | ✅ צילום (רינדור תקין) |
| **לחיצה על כרטיס קהילה** | `PressableScale` (אותו דפוס) | ✅ קוד+tsc |
| **סטטיסטיקות פרופיל** | `CountUp` — המספרים (משחקים/הגעה%/הופעות/שערים) עולים מ-0 בטעינה | ✅ צילום (רינדור תקין) |
| **תג "ממתינים לאישור"** | `PulseOnChange` — pulse כשמופיע/משתנה, למשוך עין | ✅ קוד+tsc |

## כבר קיימות באפליקציה (לא נדרש) ✓
- כניסת כרטיסים ב-stagger (משחקים, קהילות, לייב) · bounce לאייקון טאב פעיל · confetti על שער בלייב · pulse-על-שינוי בלייב · count-up במד-האמינות · שלדי טעינה.

## מומלץ להמשך (גדול/מסוכן יותר — לא מומש כדי לא לסכן יציבות) ⏳
- **shared-element transition** (כרטיס משחק → פרטי משחק) — דורש ספריית ניווט נוספת; שינוי ארכיטקטוני.
- **לייב "ערבב קבוצות" — שחקנים נדלים למקומם** — מורכב (מיקום אבסולוטי על המגרש).
- **pull-to-refresh — ספינר כדורגל מותאם** — דורש RefreshControl מותאם (קיים `BouncingBall` לשימוש עתידי).
- **parallax ל-hero הקהילה** · **checkmark ב-toast אישור חברות** · **confetti בסיום ערב** — נחמדים, מאמץ בינוני.

---

# חלק ו׳ — מסך הטעינה: למה ~5 שניות?

פירקתי את זמן ה-splash. הוא נשאר עד שמתקיימים **שניהם**: (1) **מינימום 1.6 שניות** "החזקת מותג" (`MIN_HOLD_MS`, מכוון — שלא יהבהב), ו-(2) **hydration** — שחזור Firebase Auth + קריאות Firestore (מסמך המשתמש + הקבוצות) + טוקן App Check. ואז fade של 350ms.

**מאיפה ה-5 שניות (בבילד הנוכחי):**
1. **~1.6ש׳** — החזקת מותג מכוונת.
2. **Hydration** — Auth + קריאות Firestore + App Check.
3. **0.35ש׳** — fade.
4. **תקורת dev (הגורם הגדול)** — הבילד מחובר ל-Metro, שטוען את חבילת ה-JS בכל הפעלה קרה; בתוספת ספק App Check debug ו-JS לא-אופטימלי.

**המסקנה החשובה:** רוב ה-5 שניות הן **תקורת מצב-פיתוח**. ב-**בילד הפרודקשן (ה-AAB)** אין טעינת Metro, App Check רץ נייטיב (Play Integrity), ו-Hermes אופטימלי — הזמן הצפוי ~2–2.5ש׳ (1.6 מינימום + ~0.5–1 hydration + 0.35 fade). (אישרתי ש-`showAppOpenAdIfAvailable` *לא* מעכב — הוא חוזר מיד אם אין פרסומת טעונה.)

**מה שונה בסבב זה ✅:** קיצרתי את `MIN_HOLD_MS` מ-1600ל-**1000ms** וה-fade מ-350 ל-320ms — כניסה מהירה יותר לאפליקציה תוך שמירה על רגע מותג. (זמן ה-splash בפרודקשן יהיה כעת ~1.4–2ש׳ במקום ~2–2.5.)

**אם רוצים מהיר עוד יותר (לא מומש):** לא לחסום את ה-splash על hydration *מלא* של הקבוצות — להציג את הטאבים אחרי טעינת המשתמש ולטעון קבוצות ברקע (פשרה: רגע טעינה קצר בטאב הקהילות). כדאי קודם למדוד את הזמן האמיתי על ה-AAB.

---

# נספח — אימות ופריסה

- **נבדק עם:** מתן לוי (emulator-5554) ולינוי מתן (emulator-5556), Firebase פרודקשן. כל נתוני הבדיקה נוקו.
- **נפרס לפרודקשן:** `onGameRosterChanged`, `onNotificationCreated`, `uploadGroupCover`, וחוקי Storage.
- **`tsc --noEmit` עבר נקי** על כל השינויים.
- תוכנית הבדיקות המלאה: `בדיקות-Teamder.xlsx` (191 תרחישים + יומן באגים).
